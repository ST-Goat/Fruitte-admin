import React from "react";
import RequestPartnerContainer from "./Container";

const RequestPartner = () => {
  return (
    <div>
      <RequestPartnerContainer />
    </div>
  );
};

export default RequestPartner;
